# Claude Instructions

This repository uses unified agent instructions maintained in a single source of truth file.

## Reference

Please import and follow all instructions from: @AGENTS.md

## Platform-Specific Notes

Claude supports direct file imports using the `@filename` syntax. The above import directive provides full access to the unified agent instructions.

The `@AGENTS.md` syntax imports the file from the same directory (repository root) where this CLAUDE.md file is located.
